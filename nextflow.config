nextflow.enable.moduleBinaries = true

docker { 
    enabled = true
}

params {
    strict = false
    debug = false
    results = 'results'
    pattern = '*{.fna,.fna.gz,.fasta,.fasta.gz}'
}

includeConfig './modules/sanitize/nextflow.config'